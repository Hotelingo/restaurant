# Shared contracts

Typed schemas shared between the web and API layers.

This package contains data shapes only: request/response DTOs, enums and generated contract types.
It must not contain business calculations, database access, HTTP clients, browser globals or secrets.
